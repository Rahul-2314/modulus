[project]
name = "modulus-adapter-langchain"
version = "0.1.0"
dependencies = ["modulus-sdk>=0.1.0", "langchain-core>=0.3"]
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"