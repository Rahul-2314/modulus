[project]
name = "modulus-sdk"
version = "0.1.0"
description = "Modulus fallback telemetry SDK for Python agents"
dependencies = ["httpx>=0.27"]
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"